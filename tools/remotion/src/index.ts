import { registerRoot } from "remotion";
import { Root } from "./Root";

// The entry point. Everything Remotion knows about this project it learns from
// the compositions registered here.
registerRoot(Root);
